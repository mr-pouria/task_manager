package ir.tasktop.taskTop.repo;

import ir.tasktop.taskTop.model.ValidationCodeUser;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ValidationCodeUserRepo extends JpaRepository<ValidationCodeUser, Long> {
}
